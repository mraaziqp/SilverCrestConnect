{
  "rules": {
    ".read": false,
    ".write": false,

    "applications": {
      ".indexOn": ["reference", "email", "status"]
    },
    "payments": {
      ".indexOn": ["reference", "status"]
    }
  }
}
